# Registry URL for @mentor-forge scoped packages (domain prefix required — see get-repository-endpoint).
# Auth token is injected at publish time (CI or scripts/publish-package.sh) or by `mh` locally.
# Copy to .npmrc (gitignored) when working outside CI / publish scripts.
@mentor-forge:registry=https://mentor-forge-560167829275.d.codeartifact.us-east-1.amazonaws.com/npm/mentorhub-npm/
